define("local_shopping_cart/embed_badge",["exports","core/ajax","local_shopping_cart/cart_channel"],(function(_exports,_ajax,_cart_channel){var obj;
/**
   * Keeps the embedded cart badge (count and total) in sync with the cart.
   *
   * The badge is a server-rendered snapshot. When the cart changes in another
   * same-origin context (e.g. the listing iframe beside it), the badge fetches
   * the current total and count from the get_price web service and updates itself
   * in place — no reload, and the price stays correct, not just the count. It only
   * consumes the shared channel, so there is no notify loop.
   *
   * @module     local_shopping_cart/embed_badge
   * @copyright  2026 Wunderbyte GmbH
   * @author     Georg Maißer
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.init=void 0,_ajax=(obj=_ajax)&&obj.__esModule?obj:{default:obj};const refresh=root=>{_ajax.default.call([{methodname:"local_shopping_cart_get_price",args:{usecredit:-1,useinstallments:-1,couponvalue:"",couponenabled:!1},done:function(data){const count=parseInt(data.count,10)||0;root.classList.toggle("sc-embed-badge-empty",0===count);const countel=root.querySelector('[data-region="count"]');countel&&(countel.textContent=count);const totalel=root.querySelector('[data-region="total"]');if(totalel){const price=(parseFloat(data.price)||0).toFixed(2);totalel.textContent=price+" "+(data.currency||"")}},fail:function(){}}])};_exports.init=()=>{const root=document.querySelector('[data-region="sc-embed-badge"]');root&&(refresh(root),(0,_cart_channel.onChange)((()=>refresh(root))))}}));

//# sourceMappingURL=embed_badge.min.js.map